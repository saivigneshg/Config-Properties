spring:
  boot:
    admin:
      client:
        url: http://localhost:1111

  management:
    endpoints:
      web:
        exposure:
          include: "*"

message: this is just a message from config server

server:
  port: 8082
